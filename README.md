# 日程卡片 Schedule Cards

> 在线使用：https://fjkkx77.github.io/Schedule-Cards/

一个单文件网页（`index.html`），双击就能用，不需要联网、不需要安装任何东西。
设一个日期，**自己选**是从这天起往后正数，还是距这天倒数。

---

## 一、怎么用

1. 双击 `index.html`，用浏览器打开（手机上把这个文件发给自己，用浏览器打开也一样）。
2. 点右下角蓝色 **＋** 新增一条日程：
   - **事件名称**：比如「回家过年啦」「四级」
   - **计算方式**：三选一，见下面第二节
   - **日期**：倒数时是「目标日期」，正数时是「起算日期」，标签会自己跟着变
   - **重复**：选「每年」的话，生日 / 纪念日过完会自动跳到下一年，不用每年手动改
     （只对倒数有效，选正数时这一项会自动隐藏）
   - **颜色**：6 个色，卡片顶栏和列表色块跟着变
3. **点任意一张卡片 / 一行**就能编辑，编辑弹窗里有删除按钮。

## 二、计算方式：正数 / 倒数 / 自动

| 选项 | 含义 | 例子（今天 = 9月8日） |
|---|---|---|
| **倒数** | 距离这一天还剩多少天 | 目标 12月2日 → **85 天** |
| **正数** | 从这一天起，往后一共累计过了多少天，每天 +1 | 起算 9月8日 → 今天显示**今天开始**，明天 **1 天了** |
| **自动** | 日期在未来就倒数、在过去就正数，由日期自己决定 | —— |

选了之后各种情况分别长这样：

| 情况 | 显示 |
|---|---|
| 倒数 · 还没到 | `124 天` |
| 倒数 · 就是今天 | `就是今天` |
| 倒数 · 已经过了目标日 | `7 天前` ＋「已过期」标签，数字变灰 |
| 正数 · 起算日已过 | `7 天了` ＋「已累计」标签 |
| 正数 · 起算日就是今天 | `今天开始` |
| 正数 · 起算日还没到 | `85 天后` ＋「未开始」标签，数字变灰 |

数字下面那一行会写清楚是「目标 2027-01-10 星期日」还是「起算 2026-09-01 星期二」，
不用担心看不出来这条到底是正数还是倒数。

## 三、两种显示样式

顶部「卡片 / 列表」随时切换，切换后的选择会被记住：

| 样式 | 长什么样 |
|---|---|
| **卡片** | 彩色顶栏写今天的日期，格子纸底纹，左边事件名+目标日期，右边超大天数 |
| **列表** | 一行一条，左边事件名，右边彩色天数色块，适合一屏看很多条 |

## 四、其他功能

- **排序**：自定义顺序（编辑弹窗里有「移到最前」）/ 按天数临近 / 按日期先后
- **⋯ 菜单**：
  - 列表显示日期：列表样式下要不要在事件名下面带一行目标日期
  - 主题：跟随系统 / 浅色 / 深色
  - **导出备份 / 导入备份**：导出一个 `.json` 文件，换手机换电脑时导入即可
  - 清空全部日程
- **跨零点自动刷新**：页面开着过了零点，天数会自动 −1，不用手动刷新

## 五、数据存在哪（重要）

数据存在**当前这个浏览器**的本地存储里（localStorage）。也就是说：

- 换浏览器、换设备、清理浏览器缓存/网站数据 → **数据会丢**
- 重要的日程，请用「⋯ → 导出备份」存一份 json 文件

## 六、已验证 / 未验证

**已实测**（headless Chrome 真实移动视口，2026-09-08）：

- 320 / 390 / 430 三档宽度：无横向溢出、无文字被省略号截断、所有可点元素 ≥44px
- 卡片视图、列表视图、新增/编辑弹窗、深色模式各档均已截图核对
- 功能回归：新增 → 刷新后仍在；编辑生效；删除生效；三种排序正确
- **正数/倒数六种情况逐一实测**：倒数未到 / 倒数当天 / 倒数已过期 /
  正数已累计 / 正数当天 / 正数未开始，卡片和列表两种样式下的数字、单位、标签全部核对过
- 切到「正数」时「重复」字段确实不再渲染（按元素渲染尺寸判定，不是只看 `hidden` 属性）
- 旧数据兼容：加这个功能之前存的日程（没有 mode 字段）自动按「自动」处理，显示不变
- 天数算法核对：`2027-01-10` 距 `2026-09-08` = 124 天（与参考图一致）；
  `2026-12-02` = 85 天；`2005-03-02` = 7860 天；闰日 `02-29` 的「每年」在平年自动落到 `02-28`

**没测到的**（需要真机确认）：

- 真机 iOS Safari 的实际手感、`input[type=date]` 的原生日期选择器样式
- 「添加到主屏幕」后的全屏表现（本项目没有做 PWA，按普通网页使用）
