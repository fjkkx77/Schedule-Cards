# 日程卡片 Schedule Cards

> **在线使用（推荐，支持跨设备同步）：https://rc.wbztl.xyz/**
> 备用地址：https://schedule-cards.vercel.app/
> 旧的 GitHub Pages 地址 https://fjkkx77.github.io/Schedule-Cards/ 仍可打开，但**没有同步功能**
> （同步要靠 Vercel 上的接口，GitHub Pages 只能放静态文件）

一个单文件网页（`index.html`），双击就能用，不需要联网、不需要安装任何东西。
设一个日期，**自己选**是从这天起往后正数，还是距这天倒数。

---

## 一、怎么用

1. 双击 `index.html`，用浏览器打开（手机上把这个文件发给自己，用浏览器打开也一样）。
2. 点右下角蓝色 **＋** 新增一条日程：
   - **事件名称**：比如「回家过年啦」「四级」
   - **计算方式**：三选一，见下面第二节
   - **日期**：点一下弹出日历选择器（年/月 下拉 + 「今天」+ 点日期格 + 取消/完成），
     倒数时标签是「目标日期」，正数时是「起算日期」
   - **具体时刻（可不填）**：日期下面可以选「时 : 分」，比如考试 09:00、发车 14:35。
     填了倒计时就按这个点算，不填按当天 00:00 算（和以前完全一样）。旁边有「清除」
   - **实时倒计时**：选好日期后，下面会显示「还剩 3 天 12:05:08」并逐秒跳动。
     **基准写在第二行**（填了时刻就是「距 2026-12-12 09:00」，没填就是 00:00）。颜色随紧迫度变：7 天内变橙、24 小时内变红；
     当天显示「就是今天 · 还剩 xx:xx:xx（距今天结束）」；过期显示「已过 …」；正数显示「已经 …」
   - **分组**：一行三个入口 —— 「不分组」、**中间的下拉（已有分组都收在里面，选中后按钮直接显示它）**、
     「＋ 新建分组」。已有分组不管多少个，这一行都只占一行高，不会换行把表单撑长；
     一个分组都没有时中间的下拉不出现
   - **颜色**：6 个色，卡片顶栏和列表色块跟着变
   - **置顶**：一个开关按钮，点一下在「未置顶 / 已置顶」之间切换
3. **点任意一张卡片 / 一行**就能编辑。弹窗底部：取消（左）/ 保存（右）同一排，
   「删除这条日程」单独一行在它们下面。

> **2026-09-09 去掉了「每年重复」**。生日 / 纪念日这类日程，过了当天会显示成「已过期 N 天前」，
> 不再自动跳到下一年 —— 需要的话每年手动改一次日期，或者把计算方式设成「自动」。

## 二、计算方式：正数 / 倒数 / 自动

| 选项 | 含义 | 例子（今天 = 9月8日） |
|---|---|---|
| **倒数** | 距离这一天还剩多少天 | 目标 12月2日 → **85 天** |
| **正数** | 从这一天起，往后一共累计过了多少天，每天 +1 | 起算 9月8日 → 今天显示**今天开始**，明天 **1 天了** |
| **自动** | 日期在未来就倒数、在过去就正数，由日期自己决定 | —— |

选了之后各种情况分别长这样：

| 情况 | 显示 |
|---|---|
| 倒数 · 还没到 | `124 天` |
| 倒数 · 就是今天 | `就是今天` |
| 倒数 · 已经过了目标日 | `7 天前` ＋「已过期」标签，数字变灰 |
| 正数 · 起算日已过 | `7 天了` ＋「已累计」标签 |
| 正数 · 起算日就是今天 | `今天开始` |
| 正数 · 起算日还没到 | `85 天后` ＋「未开始」标签，数字变灰 |

数字下面那一行会写清楚是「目标 2027-01-10 星期日」还是「起算 2026-09-01 星期二」，
不用担心看不出来这条到底是正数还是倒数。

## 三、两种显示样式

顶部「卡片 / 列表」随时切换，切换后的选择会被记住：

| 样式 | 长什么样 |
|---|---|
| **卡片** | 彩色顶栏写今天的日期，格子纸底纹，左边事件名+目标日期（有时刻就带上）+**下面一行农历**，右边超大天数 |
| **列表** | 一行一条，左边事件名，右边彩色天数色块，适合一屏看很多条 |

## 四、农历

顶部的今天、以及编辑弹窗里选好的日期，下面都会显示农历（如「农历七月廿八」）。

数据表取自 [data-Calculation](https://github.com/fjkkx77/data-Calculation)，
它是用**香港天文台《公曆與農曆日期對照表》1901-2100 逐日文本**生成并全区间比对过的。
**没有用浏览器内置的农历**——那个在这 200 年里有 12 段临界日算错，其中就包括 2027 年春节
（正确是 2 月 6 日，浏览器会算成 2 月 7 日）。超出 1901-2100 的日期不显示农历，不外推。

## 五、分组（堆叠卡片）

在新增/编辑弹窗里给日程填一个**分组**名（比如「考试」「生活」），留空就是不分组。
下面会列出已有的分组，点一下就能填进去，不用重复打字。

同一分组的日程会**叠成一摞**：

- **收起时**只露出最上面一张，后面每张**真的往屏幕里退**（CSS 3D 透视，不是单纯缩放），
  越往后越小、越暗、还带一点随手叠上去的歪斜，第三层再加一点虚化景深
- **按住这一摞**会整体往里压一下，松开才展开，手感像真的按到一叠卡
- **点这一摞的任意位置，或点分组标题**，卡片会依次错开 62ms 弹出来，
  带轻微过冲回弹（实测第二张位移 130px、宽度 315→367，冲到 178.6 再落回 176）
- **展开后再点某一张**才是编辑那一条 —— 收起状态下点它是展开，不会误触进编辑
- 分组标题右边的圆形箭头指示当前是收起还是展开
- 每个分组的展开/收起状态会被记住，下次打开还是你离开时的样子
- 最上面那张永远是**当前排序里排最前**的那条（比如按天数临近时，就是最紧急的那条）
- 卡片视图和列表视图都能堆叠；一个组里只有一条时不堆叠，直接点就是编辑
- 新建日程时如果填了分组，那个分组会自动展开，免得存完找不到
- **在主页管理分组**：点分组标题右边的「⋯」→ 重命名 / 删除。
  菜单底部有一行灰字提示，随当前排序变文案：
  - 「自定义顺序」下 → 「长按分组名可以直接拖动排序」
  - 其它排序下 → 「想自己排顺序：先把上面的排序切到「自定义顺序」，再长按分组名拖动」
    （长按在这些排序下本来就拖不动，不解释一句容易让人以为坏了）
  - 排顺序不给按钮，长按拖就够了；这行字只是把这个手势说出来，免得没人知道

  删除有两种，默认走安全的那种：
  - **解散分组**（左边蓝色）：里面的日程全部保留，只是变成「不分组」
  - **连同日程一起删**（右边红色）：会二次确认；走和单条删除一样的墓碑机制，能同步到另一台设备
  - 取消在这两个按钮下面单独一行
  - 重命名时如果新名字和已有分组重名，两个分组会合并（弹窗里有提示）

## 六、置顶 与 拖拽排序

**置顶**：编辑弹窗里把「置顶」打开即可。

- 置顶的日程**不管当前用哪种排序都排在最上面**，单独放在「📌 置顶」区
- 置顶区**不堆叠**，全部平铺 —— 置顶的目的就是一眼看见，再折起来就自相矛盾了
- 如果这条日程属于某个分组，置顶后会**暂时离开那个分组**（分组条数相应减 1），
  卡片上会标出「原属 XX」告诉你它本来在哪；取消置顶自动回去

**拖拽排序**（只在「自定义顺序」下）：**长按 0.45 秒**把卡片拿起来，拖到想去的位置松手。

- 三个地方都能拖：置顶区内、展开的分组内、顶层（未分组的卡片之间；
  **长按分组标题可以把整个分组挪位置**，组内日程的顺序跟着整体搬走）
- 短按还是原来的意思：收起的分组 → 展开；其它 → 打开编辑窗。长按才是拖
- 拖动期间下拉刷新自动让位，不会拖着拖着把页面刷新了

## 七、其他功能

- **排序**（5 种）：
  - 自定义顺序（长按拖动可调整）
  - 按天数临近
  - 按日期先后
  - **按创建时间**：先建的排前面
  - **按最近修改**：最近改过的排前面
  - 排序对分组同样有效：组内按它排，组与组之间按各自最靠前的那条排
- **⋯ 菜单**：
  - 列表显示日期：列表样式下要不要在事件名下面带一行目标日期
  - 主题：跟随系统 / 浅色 / 深色
  - **导出备份 / 导入备份**：导出一个 `.json` 文件，换手机换电脑时导入即可
  - 清空全部日程
- **跨零点自动刷新**：页面开着过了零点，天数会自动 −1，不用手动刷新

## 八、跨设备同步（手机 ↔ 电脑）

打开 **⋯ → 云同步**，填一个**同步码**（可以点「随机生成一个」），点「开启并同步」；
在另一台设备上填**同一个同步码**，两边的日程就自动互通了。

- 同步码相当于密码，**知道它的人就能看到并修改这些日程**，别外传。服务端只存它的 sha256 哈希
- 只同步日程本身。视图（卡片/列表）、排序、主题、分组展开状态是每台设备自己的偏好，不同步
- **什么时候会同步**：打开页面 / 从后台切回页面 / 窗口获得焦点 / 网络恢复 /
  每 30 秒（仅在页面可见时）/ 本地改动后 1.2 秒
- **这是秒级同步，不是即时推送**：手机上改完，电脑那边要切回页面或最多等 30 秒
- **合并规则**：按条目 id 取并集，同一条取修改时间更新的那份。
  删除是打「墓碑」标记而不是真删（否则另一台设备会把删掉的当新条目加回来），墓碑保留 120 天
- 两台设备同时改也不会丢东西：写入带版本号，冲突时自动重新合并并重试
- 顶部会显示同步状态（已同步 / 同步中 / 失败原因）
- **开启同步后，没被你编辑过的示例数据会自动退场**（不上传、本地也清掉）。
  因为示例在每台设备上「看着一样但内部 id 不同」，参与合并只会变成每条两份。
  你动过的示例卡片就算你自己的数据，照常同步

技术上：Vercel Functions（`api/sync.js`，零依赖）+ Upstash Redis。
免费额度 500K 命令/月、256MB、10GB 流量，这个用量下用不掉零头。

## 九、数据存在哪（重要）

**没开同步时**：数据只存在**当前这个浏览器**的本地存储里（localStorage）。也就是说：

- 换浏览器、换设备、清理浏览器缓存/网站数据 → **数据会丢**
- 重要的日程，请用「⋯ → 导出备份」存一份 json 文件

**开了同步后**：数据同时存在本机和云端；换设备只要填同一个同步码就能取回。
但导出备份仍然值得做——同步码丢了、或者误删了全部日程，本地备份是最后一道保险。

## 十、已验证 / 未验证

**已实测**（headless Chrome 真实移动视口，2026-09-08）：

- 320 / 390 / 430 三档宽度：无横向溢出、无文字被省略号截断、所有可点元素 ≥44px
- 卡片视图、列表视图、新增/编辑弹窗、深色模式各档均已截图核对
- 功能回归：新增 → 刷新后仍在；编辑生效；删除生效；三种排序正确
- **正数/倒数六种情况逐一实测**：倒数未到 / 倒数当天 / 倒数已过期 /
  正数已累计 / 正数当天 / 正数未开始，卡片和列表两种样式下的数字、单位、标签全部核对过
- 切到「正数」时「重复」字段确实不再渲染（按元素渲染尺寸判定，不是只看 `hidden` 属性）
- 旧数据兼容：加这些功能之前存的日程（缺 mode / group / 创建时间字段）会自动补齐，显示不变
- **分组堆叠**：收起时的露出量（卡片 26px、列表 12px）、透视投影后各层宽度（390px 下 367→315）、
  展开动画逐帧采样（130px 位移 + 过冲回弹 + 约 640ms 收敛）、按住时整摞压到 0.972、
  收起态点击是展开而展开态点击是编辑、展开状态刷新后保持、新建入组后自动展开
  —— 均在 320 / 390 / 430 三档实测
- **两种新排序**：按创建时间＝先建的在前；按最近修改＝刚改过的排第一，均已实测
- **跨设备同步（线上真实环境 rc.wbztl.xyz + 真实 Redis 实测）**：手机上传 → 电脑拉到 →
  电脑新增 → 手机收到 → 手机删除 → 电脑同步到删除；本地模拟环境还额外验过
  「删除不回流」和「两台设备并发新增后两边都拿到全部条目」；同步弹窗在 320/390 下无溢出
- **服务端没配存储时**会显示可读的中文原因，而不是白屏或报错
- **农历（2026-09-09）**：关键验证 2027-02-06 → 农历正月初一（浏览器内置历法会错算成 2/7）；
  2026-09-09 → 七月廿八、2027-01-10 → 腊月初三；顶部与编辑弹窗两处都正确
- **在主页管理分组（2026-09-09）**：⋯ 菜单两项；重命名后 2 条日程跟着走、展开状态也跟着改名；
  「解散分组」后日程一条不少全变成不分组；「连同删除」后进墓碑（可同步）；
  标题栏三个可点区域高度都是 44px；320/390 无横向溢出
- **＋ 按钮位置（2026-09-09）**：1707px 宽屏下按钮落在内容列**外侧**空白里
  （左边缘距列右边缘正好 16px、离屏幕右边 480px，**不压住任何卡片**）；
  屏幕不够宽时自动回退到"离右边 16px"，与手机上一致
- **可选时刻 + 主页农历（2026-09-09）**：设了时刻的卡片副标题显示「目标 2026-12-12 星期六 09:00」，
  下面单独一行显示农历；列表视图接在同一行。倒计时基准跟着时刻走（改 09:00→14:35 基准同步变、
  保存进数据、卡片跟着显示）；**旧数据没有 time 字段时按钮显示 --:--、清除键隐藏、基准仍是 00:00**（向后兼容）；
  设了再清除能正确回到 00:00
- **实时倒计时（2026-09-09）**：远期/7天内/24小时内/就是今天/已过期/正数累计 六种情形的文案与配色
  逐一实测；秒确实在跳；切换计算方式立刻改口径；**用包裹 setInterval 的计数器实测确认
  关掉弹窗后计时器真的停了、反复开关不会叠加**（不是"我写了 clearInterval"就算数）；
  320/390 下都只占一行、无横向溢出
- **删除分组弹窗按钮布局**：解散（左）/ 连同删除（右）同一排、取消单独一行在下面；
  重命名弹窗仍是取消（左）/ 保存（右）一排；三档文案都不换行、功能未受影响
- **分组下拉（2026-09-09）**：6 个分组时这一行仍只有 44px 高（改之前会换行）；
  下拉里当前分组高亮、选完按钮直接显示它并自动收起；点表单别处会收起；
  没有任何分组时整个下拉不渲染；保存写入正确 —— 320/390 两档实测
- **表单四项改动（2026-09-09）**：重复字段已移除且存量数据里的 repeat 字段一并清掉；
  分组的"选已有 / 新建"两种模式确实互斥（选择时输入框隐藏、新建时选项列表隐藏）；
  置顶开关点一下在两种状态间正确切换；按钮布局实测取消 left=18、保存 left=200、同一排，
  删除单独整行(354px)在下方且是红字文案 —— 320/390 两档无溢出、无 <44px 触摸目标
- **日期选择器**（移植自 data-Calculation）：年/月 下拉打开时当前值自动滚到可视区、
  选完自动收起；「今天」按钮回到今天；取消不改值、完成才回填；
  月份天数正确（2027 年 2 月 28 天）、周一开头对齐、**1月31日切到2月自动收成28号**；
  320/390/430 三档日期格高度均为 44px，无横向溢出；深浅色都看过
- **下拉刷新**：8 个手势用例全绿（起手抖 6/12px 再上滑、纯上滑、横滑、下拉 60、下拉 150、
  下拉一半改上滑 → 均不刷新且指示器归位；下拉 240 → 刷新），下拉过程逐帧取样确认跟手
- **置顶**：置顶区平铺不堆叠、原分组条数相应减少、卡片带「原属 XX」标记、
  换成「按天数临近」后仍排最前、取消置顶回到原分组 —— 均已实测
- **拖拽排序**：置顶区内与展开的分组内各拖一次，顺序互换且 order 正确写回、刷新后保持；
  拖动期间没有误触发下拉刷新；短按仍然是编辑/展开
- **整组拖动（2026-09-10）**：长按分组标题 0.65 秒把「甲组」整组拖到「乙组」后面，
  顺序变为 乙→甲→丙、组内 4 条日程的 order 全部跟着重排、刷新后保持
- **⋯ 设置菜单的定位（2026-09-10）**：原来 CSS 写死 `right:12px`（贴视口右缘），
  手机上碰巧就在按钮下面所以一直没暴露；宽屏内容列居中后按钮在 x≈1140，菜单却飞到 1695，差 555px。
  改成 `openMenu()` 里按按钮 rect 算，菜单右缘对齐按钮右缘、下移 8px、两侧各留 12px；
  转屏/改窗口大小时重算。1707/1280/430/390/320 五档实测右缘错位 ≤1px、无溢出、菜单项均 ≥44px
- **标题字号（2026-09-10）**：站名从 19px 改成 `clamp(28px,8vw,36px)`
  （320→28、390→31.2、430→34.4、宽屏封顶 36），「今天」行 13→14px、间距拉开，
  ⋯ 按钮改为与标题首行对齐；四档实测标题与副标题都不换行、不与 ⋯ 重叠、无横向溢出
- **⋯ 菜单（2026-09-10）**：只剩重命名/删除两项，按钮高度 44px；提示行按排序切换文案
  （自定义顺序 → "长按可拖"；按天数临近 → "先切到自定义顺序"）；重命名弹窗仍能正常打开；
  **菜单宽度固定 240px、左右各留 12px，320/390/430 三档均无溢出**
  —— 这里踩过一个坑：提示是可任意断行的中文长串，`max-width` 下菜单的收缩宽度会等于
  "视口宽 − left"，而 left 又由测得的宽度算出，循环依赖导致菜单被顶到屏幕右边缘；改成写死宽度才稳
- 天数算法核对：`2027-01-10` 距 `2026-09-08` = 124 天（与参考图一致）；
  `2026-12-02` = 85 天；`2005-03-02` = 7860 天

**没测到的**（需要真机确认）：

- 真机 iOS Safari 的实际手感、`input[type=date]` 的原生日期选择器样式
- 「添加到主屏幕」后的全屏表现（本项目没有做 PWA，按普通网页使用）
- **手机不挂代理时能不能稳定打开**：Vercel 官方声明不保证中国大陆的可用性与速度，
  用自有域名比 `*.vercel.app` 被拦的概率低，但没有保证，需要在真机上自己确认
